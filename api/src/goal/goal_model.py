class Goal:

    def __init__(self, goal_id, user_id, goal_name, tasks_list):
        self.goal_id = goal_id
        self.user_id = user_id
        self.goal_name = goal_name
        self.tasks_list = tasks_list
